# paas-ansible-workspace
This ansible project is to configure my local workspace 

# Run the playbook:
 - sudo ansible-playbook -u myuser -b playbook.yml
 - sudo ansible-playbook -u myuser -b -c local playbook.yml
